/**
 * Host adapter for Expo's bare-expo-only test module. The upstream screen
 * checks for its native module and renders its own unavailable-state content.
 */
export function MyCustomView() {
  return null;
}

export function customBorder() {
  return undefined;
}
